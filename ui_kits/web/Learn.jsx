// Veridian Markets — Learn VM page.
// Blank scaffold. Build the Learn content below; the rail, index strip, and
// footer are provided by the app shell, so just fill this container.
function Learn({ go }) {
  return (
    <div style={{ padding:'26px 32px 60px', maxWidth:1180, margin:'0 auto' }}>
      <Kicker>LEARN · VM</Kicker>
      <h1 style={{ fontFamily:VM.serif, fontWeight:700, fontSize:32, lineHeight:1.07, margin:'10px 0 0' }}>Learn.</h1>

      {/* ───────── Build the Learn page here ───────── */}

    </div>
  );
}

Object.assign(window, { Learn });
